#ifndef _HASH_TABLE_H__
#define _HASH_TABLE_H__

#include "my_list.h"
/*
struct hashtable_elem_t {
    const char* string;
    size_t call_counter;
};

struct bucket_t {
    list_t data;
    size_t size;
};

struct hashtable_t {
    bucket_t* buckets;
    size_t size;
};


int init();
int search();
int insert();
int dstr();

size_t hash(const char* string);

//int getSize();
//int bucketSize();

*/
#endif
